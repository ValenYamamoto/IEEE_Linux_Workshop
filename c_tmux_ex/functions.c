#include <stdio.h>
#include "functions.h"

void printHelloWorld() {
		printf( "Hello World\n" )
}

int fibonacci( int index ) {
		if( index < 2 ) 
				return inde;
		return fibonacci( index -1 ) + fibonacci( index - 2 );
}
